---
# enable swap file
system_swap_enabled: true

# in megabytes
system_swap_size: 1024

# swap file path
system_swap_path: /var/swap

# sysctl.conf
system_swap_sysctl:
  - name: vm.swappiness
    value: 10
  - name: vm.vfs_cache_pressure
    value: 100
